{
  "git": {
    "commitMessage": "chore(package-c): release v${version}",
    "tagName": "package-c@v${version}",
    "tagAnnotation": "Release package-c v${version}",
    "requireBranch": ["main", "release/*"]
  },
  "github": {
    "release": true,
    "releaseName": "package-c v${version}",
    "autoGenerate": true
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "after:git:release": "bash ../../scripts/git-flow-merge.sh ${version}"
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  }
}
